---
'eslint-plugin-no-server-imports': minor
---

Add TanStack Start framework support with auto-detection, default client/server file patterns, and a `recommended-tanstack-start` config preset. Also widen ESLint peer dependency to `^9.0.0 || ^10.0.0` and replace deprecated `context.getFilename()`/`context.getSourceCode()` calls.
